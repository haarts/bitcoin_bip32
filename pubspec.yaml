name: bitcoin_bip32
version: 0.1.2
description: Library implementing Bitcoins BIP32 (Hierarchical Deterministic key derivation) specification in a Flutter friendly fashion.
author: Harm Aarts <harm@aarts.email>
homepage: https://github.com/haarts/bitcoin_bip32

dependencies:
    pointycastle: ^1.0.0
    base58check: ^1.0.1
    convert: ^2.1.0

dev_dependencies:
    pedantic: ^1.9.0
    test: ^1.5.1

environment:
    sdk: ">=2.3.0 <3.0.0"
